services:
  db:
    image: postgres:16
    container_name: fundraising-db
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: fundraising
    ports:
      - "5432:5432"
    volumes:
      - fundraising_pgdata:/var/lib/postgresql/data

volumes:
  fundraising_pgdata:

